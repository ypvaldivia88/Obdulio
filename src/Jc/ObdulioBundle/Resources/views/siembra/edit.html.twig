{% extends 'layout.html.twig' %}

{% block wrapper %}
    {{ parent() }}
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <h1>
                Editar Planificacion de la Siembra
                {#<small>it all starts here</small>#}
            </h1>
            <ol class="breadcrumb">
                <li><a href="{{ path('inicio') }}"><i class="glyphicon glyphicon-home"></i> Inicio</a></li>
                <li><a href="{{ path('siembra_index') }}">Planificaci&oacute;n de la Simbra</a></li>
                <li><a class="active">Editar</a></li>
            </ol>
        </section>

        <!-- Main content -->
        <section class="content">

            <!-- Default box -->
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">Editar</h3>
                </div>
                <div class="box-body">
                    {{ form_start(edit_form, { 'attr' : {'novalidate': 'novalidate', 'role' : 'form'} }) }}
                    <fieldset>
                        <div class="form-group col-md-6">
                            <strong>Producto</strong>
                            {{ form_widget(edit_form.fkProducto, {'attr': {
                                'class': 'form-control select2',
                                'style' : 'width:100%'

                            }}) }}
                            <span class="text-danger">{{ form_errors(edit_form.fkProducto) }}</span>
                        </div>
                        <div class="form-group col-md-6">
                            <strong>Unidad</strong>
                            {{ form_widget(edit_form.fkUnidad, {'attr': {
                                'class': 'form-control select2',
                                'style' : 'width:100%'

                            }}) }}
                            <span class="text-danger">{{ form_errors(edit_form.fkUnidad) }}</span>
                        </div>
                        {% if not form_errors(edit_form.enero) %}
                            <div class="form-group col-md-6">
                                <strong>Enero</strong>
                                {{ form_widget(edit_form.enero, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Enero',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.enero) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Enero</strong>
                                {{ form_widget(edit_form.enero, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Enero',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.enero) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajeenero') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.febrero) %}
                            <div class="form-group col-md-6">
                                <strong>Febrero</strong>
                                {{ form_widget(edit_form.febrero, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Febrero',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.febrero) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Febrero</strong>
                                {{ form_widget(edit_form.febrero, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Febrero',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.febrero) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajefebrero') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.marzo) %}
                            <div class="form-group col-md-6">
                                <strong>Marzo</strong>
                                {{ form_widget(edit_form.marzo, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Marzo',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.marzo) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Marzo</strong>
                                {{ form_widget(edit_form.marzo, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Marzo',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.marzo) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajemarzo') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.abril) %}
                            <div class="form-group col-md-6">
                                <strong>Abril</strong>
                                {{ form_widget(edit_form.abril, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Abril',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.abril) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Abril</strong>
                                {{ form_widget(edit_form.abril, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Abril',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.abril) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajeabril') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.mayo) %}
                            <div class="form-group col-md-6">
                                <strong>Mayo</strong>
                                {{ form_widget(edit_form.mayo, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Mayo',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.mayo) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Mayo</strong>
                                {{ form_widget(edit_form.mayo, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Mayo',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.mayo) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajemayo') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.junio) %}
                            <div class="form-group col-md-6">
                                <strong>Junio</strong>
                                {{ form_widget(edit_form.junio, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Junio',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.junio) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Junio</strong>
                                {{ form_widget(edit_form.junio, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Junio',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.junio) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajejunio') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.julio) %}
                            <div class="form-group col-md-6">
                                <strong>Julio</strong>
                                {{ form_widget(edit_form.julio, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Julio',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.julio) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Julio</strong>
                                {{ form_widget(edit_form.julio, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Julio',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.julio) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajejulio') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.agosto) %}
                            <div class="form-group col-md-6">
                                <strong>Agosto</strong>
                                {{ form_widget(edit_form.agosto, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Agosto',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.agosto) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Agosto</strong>
                                {{ form_widget(edit_form.agosto, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Agosto',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.agosto) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajeagosto') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.septiembre) %}
                            <div class="form-group col-md-6">
                                <strong>Septiembre</strong>
                                {{ form_widget(edit_form.septiembre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Septiembre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.septiembre) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Septiembre</strong>
                                {{ form_widget(edit_form.septiembre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Septiembre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.septiembre) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajeseptiembre') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.octubre) %}
                            <div class="form-group col-md-6">
                                <strong>Octubre</strong>
                                {{ form_widget(edit_form.octubre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Octubre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.octubre) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Octubre</strong>
                                {{ form_widget(edit_form.octubre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Octubre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.octubre) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajeoctubre') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.noviembre) %}
                            <div class="form-group col-md-6">
                                <strong>Noviembre</strong>
                                {{ form_widget(edit_form.noviembre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Noviembre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.noviembre) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Noviembre</strong>
                                {{ form_widget(edit_form.abril, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Noviembre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.noviembre) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajenoviembre') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.diciembre) %}
                            <div class="form-group col-md-6">
                                <strong>Diciembre</strong>
                                {{ form_widget(edit_form.diciembre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Diciembre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.diciembre) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Diciembre</strong>
                                {{ form_widget(edit_form.diciembre, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el plan de Diciembre',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.diciembre) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajediciembre') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        {% if not form_errors(edit_form.anno) %}
                            <div class="form-group col-md-6">
                                <strong>Ano</strong>
                                {{ form_widget(edit_form.anno, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el Ano',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '7'
                                }}) }}
                            </div>
                        {% endif %}
                        {% if form_errors(edit_form.anno) %}
                            <div class="form-group has-error col-md-6">
                                <strong>Ano</strong>
                                {{ form_widget(edit_form.anno, {'attr': {
                                    'class': 'form-control',
                                    'placeholder' : 'Escriba el ano',
                                    'onKeyPress' : 'return OnlyNumbers(event);',
                                    'maxlength' : '10'
                                }}) }}
                                <span class="text-danger">{{ form_errors(edit_form.anno) }}</span>
                                {% for flashMessage in app.session.flashbag.get('mensajeano') %}
                                    <span class="text-danger">{{ flashMessage }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}

                    </fieldset>
                    <hr>
                    <br>

                    <div class="col-md-4"> </div>
                    <div class="col-md-4">
                        <div class="col-md-6">

                            {{ form_widget(edit_form.save, {'label' : ' Guardar', 'attr': {'class': 'fa fa-save btn btn-warning pull-right'}}) }}
                        </div>
                        <div class="col-md-6">
                            <a href="{{ path('siembra_index') }}" class="btn btn-sm btn-danger">
                                <i class="fa fa-ban"></i> <strong>Cancelar</strong>
                            </a>
                        </div>
                    </div>
                    <div class="col-md-4"> </div>
                    <p>&zwnj;</p>
                    <p>&zwnj;</p>
                    <br>
                    {{ form_end(edit_form) }}
                </div>
                <!-- /.box-body -->
                {#<div class="box-footer">
                  Footer
                </div>#}
                <!-- /.box-footer-->
            </div>
            <!-- /.box -->

        </section>
        <!-- /.content -->
    </div>
    <input type="hidden" value="siembras" id="seleccion">
{% endblock %}

{% block javascripts  %}
    {{ parent() }}
    <script src="{{ asset('public/select2/dist/js/select2.full.min.js') }}"></script>
    <script>
        $(function () {
            //Initialize Select2 Elements
            $('.select2').select2()
        })
    </script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('public/select2/dist/css/select2.min.css') }}">
{% endblock %}
