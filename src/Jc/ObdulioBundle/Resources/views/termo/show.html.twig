{% extends 'base.html.twig' %}

{% block body %}
    <h1>Termo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ termo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ termo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('termo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('termo_edit', { 'id': termo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
